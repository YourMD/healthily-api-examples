info:
  name: Initial query
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/initial"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |-
      {
          "gender": "male",
          "age": 44,
          "initial_query": "headache"
      }
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
