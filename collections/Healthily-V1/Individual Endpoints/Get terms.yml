info:
  name: Get terms
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/{{api_subpath}}/terms"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  auth:
    type: bearer
    token: "{{v5_token}}"

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("terms_version", res.getBody().content_version);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
