info:
  name: Step Back
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/step-back"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
