info:
  name: Get one article
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/{{api_subpath}}/article/con-alcohol-how-much-am-i-really-drinking"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
