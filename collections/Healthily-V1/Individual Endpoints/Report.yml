info:
  name: Report
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/{{api_subpath}}/report"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
