info:
  name: Search
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/{{version}}/symptom-search?text=ear ache"
  headers:
    - name: x-api-key
      value: "{{x_api_key}}"
  params:
    - name: text
      value: ear ache
      type: query
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
