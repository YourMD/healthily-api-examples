info:
  name: Symptoms 6
  type: http
  seq: 16

http:
  method: POST
  url: "{{base_url}}/{{version}}/chat"
  headers:
    - name: x-api-key
      value: "{{x_api_key}}"

  body:
    type: json
    data: |-
      {
          "type": "symptoms",
          "included": [],
          "excluded": ["C0007859", "C0028643", "C0162298", "C0036572"]
      }
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
