info:
  name: Symptoms 2
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/chat"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |-
      {
          "type": "symptoms",
          "included": ["C0239206", "C2316035"],
          "excluded": []
      }
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
