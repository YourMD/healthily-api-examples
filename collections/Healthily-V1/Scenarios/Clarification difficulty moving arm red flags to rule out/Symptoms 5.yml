info:
  name: Symptoms 5
  type: http
  seq: 17

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/chat"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |-
      {
          "type": "symptoms",
          "included": [],
          "excluded": ["C0004604", "C0311394", "C0003862", "C0239833"]
      }
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
