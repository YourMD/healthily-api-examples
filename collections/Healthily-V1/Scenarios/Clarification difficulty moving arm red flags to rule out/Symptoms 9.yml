info:
  name: Symptoms 9
  type: http
  seq: 22

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/chat"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |+
      {
          "type": "symptoms",
          "included": [],
          "excluded": ["C0241166", "C0033774", "C0683369", "C0231710"]
      }

  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
