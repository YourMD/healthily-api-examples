info:
  name: Symptoms 10
  type: http
  seq: 23

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/chat"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |+
      {
          "type": "symptoms",
          "included": [],
          "excluded": ["C0003467", "C0002622", "C0003537", "CM000411"]
      }

  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
