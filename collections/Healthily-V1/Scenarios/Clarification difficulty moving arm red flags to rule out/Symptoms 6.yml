info:
  name: Symptoms 6
  type: http
  seq: 18

http:
  method: POST
  url: "{{base_url}}/{{version}}/chat"
  headers:
    - name: x-api-key
      value: "{{x_api_key}}"
  body:
    type: json
    data: |-
      {
          "type": "symptoms",
          "included": [],
          "excluded": ["C0004604", "C0311394", "C0015732", "C0028961"]
      }
  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
