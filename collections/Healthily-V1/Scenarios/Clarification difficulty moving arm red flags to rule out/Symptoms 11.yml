info:
  name: Symptoms 11
  type: http
  seq: 24

http:
  method: POST
  url: "{{base_url}}/{{api_subpath}}/chat"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |+
      {
          "type": "symptoms",
          "included": [],
          "excluded": ["C0013428", "C0011053", "C0011253", "C0018524"]
      }


  auth:
    type: bearer
    token: "{{v5_upgraded_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
